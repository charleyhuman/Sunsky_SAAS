<!DOCTYPE html>
<html>
<head>
  <title>Fire Department Training Record Program</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
 <body>
    <header class="navbar navbar-fixed-top">
      <div class="container">
        <a id="logo" href="/"> Fire Department Training Program</a>
        
        <nav>
          <ul class="nav navbar-nav navbar-right">
          <% if false %>
            <li><%= link_to "Sign up", signup_path %></li> 
            <% end %>
            <% if logged_in? %>
            <li id="fat-menu" class="dropdown">
              <a id="menu_controller" href="#" class="dropdown-toggle" data-toggle="dropdown">
                 Howdy <%= @current_user.name.split(" ")[1] %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", url_for(:controller => :sessions, :action => :profile) %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", logout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
          <a href="/login" id="sign_in_menu">Sign in</a>
          <% end %>
          </ul>
        </nav>
      </div>
    </header>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
