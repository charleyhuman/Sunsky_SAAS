
<body>

<div class="container">          
  <table class="table table-hover sortable">
    <thead>
      <tr>
            <th>Name</th>
            <th>Class Number</th>
            <th>CE Hours</th>
            <th>Instructor</th>     
            <th>Location</th>
            <th>Action</th>
      </tr>
    </thead>
    <tbody>
            <% @class_info_total.each do |x| %>
              <tr>
                  <td><%= link_to x.class_name, class_info_path(x), :method => 'get' %></td>
                  <td><%= x.class_number %></td>
                  <td><%= x.ce_hours %></td>
                  <td><%= x.lead_instructor %></td>
                  <td><%= x.class_location %></td>
                  <td><button class="btn"><%= link_to "delete", class_info_path(x), :method => 'delete', :data => {confirm: "Are you sure?"} %></button></td>
              </tr>
            <% end %>
    </tbody>
  </table>
</div>

</body>